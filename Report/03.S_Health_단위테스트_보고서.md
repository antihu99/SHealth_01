# 03. S_Health 단위테스트 보고서

_작업시나리오 3단계 · SHealth BMI (Java) · 2026-05-20_

**계획서:** [`docs/03.S_Health_단위테스트계획서.md`](../docs/03.S_Health_단위테스트계획서.md)

---

## 1. 계획 대비 수행 요약

| 구분 | 계획 | 수행 | 비고 |
|------|------|------|------|
| 테스트 클래스 | 5개 `*Test` + 기존 1 | ✅ 6 클래스 | `TestFixtures` 헬퍼 추가 |
| TS-01~06 | 전 항목 | ✅ | TS-05 빈 나이대는 **NaN** 으로 기대값 수정 |
| 픽스처 | 4 CSV | ✅ `src/test/resources/fixtures/` | |
| 프로덕션 수정 | 최소 | ✅ 3 메서드 package-private·`computeBmiFromKgAndCm` 추출 | public API 유지 |
| 4단계 TC | 미작성 | ✅ | — |

---

## 2. 테스트 클래스·메서드 목록 (TS 매핑)

| TS | 테스트 클래스 | 주요 메서드 | 건수 |
|----|---------------|-------------|------|
| TS-01 | `SHealthBmiCalculationTest` | `computeBmi_readmeExample`, `computeBmi_parameterized`, `computeBmi_usesMeterConversion` | 5 |
| TS-02·TS-04 | `SHealthWeightImputationTest` | `calculateBmi_imputesZeroWeight…`, `belongsToAgeGroup_ageBoundaries` (×10), `belongsToAgeGroup_age19…` | 12 |
| TS-03 | `SHealthBmiClassificationTest` | `classifyBmiCategoryIndex_boundaryValues` (×9), `classifyBmiCategoryIndex_representativeMidpoints` (×4) | 13 |
| TS-06 | `SHealthExceptionTest` | `calculateBmi_missingFile…`, `calculateBmi_invalidAge…`, `calculateBmi_headerOnly…` | 3 |
| TS-05 | `SHealthAgeGroupRatioTest` | `getBmiRatio_singleNormal…`, `getBmiRatio_emptyAgeGroup_returnsNaN` | 2 |
| 회귀 | `SHealthBMITest` | `getBmiRatio_matchesLegacyBaseline…` | 1 |
| **합계** | | | **36** |

---

## 3. 경계값·픽스처 설계 (계획 대비)

| 항목 | 계획 | 실제 | 변경 이유 |
|------|------|------|-----------|
| BMI=25 분류 | -1 (레거시) | 동일 | `classifyBmiCategoryIndex` 레거시 갭 유지 |
| 빈 나이대 비율 | 0% 가정 | **NaN** | `usersInAgeGroup==0` 시 `0.0/0.0` — 현 구현 고정 |
| 파일 없음 | userCount=0 | 동일 + 비율 NaN | IOException 삼킴·`printStackTrace` (개선은 4단계 이슈) |

### 픽스처

| 파일 | 용도 |
|------|------|
| `impute_20s_three.csv` | 체중 0 → 평균 70kg, 20대 3분류 각 33.33% |
| `single_normal_20s.csv` | 20대 정상 100% |
| `bad_age.csv` | `NumberFormatException` |
| `header_only.csv` | userCount=0 |

---

## 4. Red→Green 과정에서 발견한 결함

| ID | 내용 | 조치 |
|----|------|------|
| D-01 | 빈 나이대 `getBmiRatio` → **NaN** (명세상 0% 기대 가능) | TC를 현 동작에 맞게 수정; 4단계에서 방어 코드 검토 권장 |
| D-02 | BMI **정확히 25** 는 어느 분류에도 미포함 (`-1`) | 레거시 유지·TC로 문서화 (요구사항 §5.3 vs 구현 갭) |
| D-03 | 파일 없음 시 `printStackTrace`만 출력 | TC는 userCount=0 검증; 예외 전파는 별도 개선 이슈 |

신규 프로덕션 결함 수정은 하지 않았으며, **테스트로 현 동작을 고정**했습니다.

---

## 5. mvn clean test 결과

| 항목 | 값 |
|------|-----|
| 실행 일시 | 2026-05-20 |
| 명령 | `mvn clean test` |
| Tests run | **36** |
| Failures | **0** |
| Errors | **0** |
| Skipped | **0** |
| 결과 | **Green** |

---

## 6. 4단계 전 보강 권장 TC

| 우선순위 | 항목 | 설명 |
|----------|------|------|
| 높음 | 빈 나이대 0% 반환 | `storeRatioPercentages`에서 `usersInAgeGroup==0` 가드 |
| 높음 | BMI=25 분류 | 명세(≥25 비만)와 레거시(`>25`만 비만) 정합 |
| 중간 | IOException 처리 | `readUsersFromFile` 실패 시 호출자 전파 또는 Optional |
| 중간 | height==0 보정 | 4단계 신규 요구(TS 확장) |
| 낮음 | 정상 BMI 목록·전체 비율 | 4단계 API 추가 후 전용 `*Test` |

---

## 부록: Given–When–Then 예시

```java
@Test
@DisplayName("체중 0 사용자에게 동일 20대 평균 체중(70kg)이 적용되어 과체중 비율이 산출된다")
void calculateBmi_imputesZeroWeightWithAgeGroupAverage() {
  // Given — 60·80·0 → 평균 70
  SHealth shealth = new SHealth();
  String fixture = TestFixtures.path("impute_20s_three.csv");

  // When
  int count = shealth.calculateBmi(fixture);

  // Then
  assertEquals(3, count);
  assertEquals(33.3333, shealth.getBmiRatio(20, SHealth.TYPE_OVERWEIGHT), 1e-3);
}
```

_(End of Document)_
